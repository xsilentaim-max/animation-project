# animation-project

A simple HTML + CSS animation featuring orbiting dots and a pulsing core.

## Run locally

```bash
python -m http.server 8000
```

Then open `http://localhost:8000` in your browser.
